# skp-validator-jsonschema

Generate standard JSON Schemas from your `skp-validator` rules.

Integrates with `schemars` to automatically enrich generated schemas with your validation constraints like `minLength`, `minimum`, `pattern`, and `format`.
